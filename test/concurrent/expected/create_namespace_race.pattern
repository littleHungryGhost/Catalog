# create_namespace_race expected patterns
# At least one session succeeded
+ .*"namespace".*
# The losing session raised an error (schema already exists / conflict)
+ .*ERROR:.*
# No internal errors
! .*P0009.*
# No connection failures / crashes
! .*server closed.*
! .*connection lost.*
! .*Connection refused.*
